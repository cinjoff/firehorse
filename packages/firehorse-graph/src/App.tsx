/**
 * Placeholder shell. The graph itself lands once the exploration shell is
 * designed (#91) and the supermemory proxy exists (#92); this package only
 * has to build, typecheck and run until then.
 */
export function App() {
  return (
    <main className="app">
      <h1>Firehorse memory graph</h1>
      <p>
        Nothing to draw yet. This package is scaffolding — the graph arrives with the exploration
        shell and the supermemory proxy.
      </p>
    </main>
  );
}
